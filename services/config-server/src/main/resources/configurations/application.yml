spring:
  cloud:
    config:
      override-system-properties: false
name:
  value: ms
management:
  zipkin:
    tracing:
      endpoint: http://zipkin-service:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0