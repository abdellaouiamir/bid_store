apiVersion: v1
kind: Service
metadata:
  name: config-service
  labels:
    app: config-deployment
spec:
  selector:
    app: config-deployment
  ports:
    - port: 8888          # External port
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-deployment
  labels:
    app: config-deployment
spec:
  replicas: 1          # Number of pod replicas
  selector:
    matchLabels:
      app: config-deployment
  template:
    metadata:
      labels:
        app: config-deployment
    spec:
      containers:
        - name: config-deployment
          image: abdellaouiamir/config-server:1.0.2
          ports:
            - containerPort: 8888
          resources:
            requests:
              memory: "256Mi"
              cpu: "0.2"
            limits:
              memory: "512Mi"
              cpu: "0.5"